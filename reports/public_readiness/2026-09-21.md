# Public repository audit — 21 September 2026

Repository: `xia-geom/math_video_project` (already public). Work is in PR #14;
this report is not a declaration that the PR has merged or that administration
settings have been changed.

## Executed baseline scan

[Successful GitHub Actions run 35565917319](https://github.com/xia-geom/math_video_project/actions/runs/35565917319)
ran against merge snapshot `107ab38cfd29d21680df4fcc561be74edb2c06e7`.
Gitleaks 8.30.1 was verified against its official Linux archive SHA-256 before
execution. Six synthetic audit tests also passed in that run.

| Scope | Measured result |
| --- | --- |
| Fetched reachable history | 329 commits, 47 refs, 1,123 distinct blobs |
| Text inventory | 1,012 text blobs examined; no oversized blobs skipped |
| Credential-like historical filenames | None found by the filename heuristic |
| Gitleaks history scan | Zero findings |
| Gitleaks tracked-file scan | Zero findings |
| Public issue/PR bodies | 14 examined |
| Issue and inline review comments | 4 and 0 examined respectively |
| Workflow log sample | 16 job logs across the latest 10 completed runs, out of 218 completed runs available |
| Gitleaks sampled GitHub text scan | Zero findings |
| Artifact metadata | 100 of 159 records; payloads not examined |

The inventory also flagged local-path patterns in 13 distinct file locations,
including synthetic test/scanner code and historical paths, and counted two
non-noreply commit email identities. These are review indicators, not 13 private
data leaks or evidence that credentials were exposed. Actual matched values are
not reproduced here. One sampled GitHub text file matched the local-path pattern.
No history rewrite, credential rotation or deletion was performed.

## Implemented safeguards in this PR

- Repeatable read-only audit on every pull request and main push; raw downloaded
  logs and scanner matches remain temporary, only a sanitized summary is retained.
- Full commit pins, no persistent checkout credentials, read-only permissions,
  bounded execution and cancellation for the existing smoke workflow; its 20
  scenes and failing lint checks are not removed.
- The teaching, UQAM revision and UQAM ouvertures workflows expose Speech secrets
  only for explicit manual narration requests on `main`. Pull requests remain
  credential-free; configured voices and source content are unchanged.
- Remove custom status-writing credentials from UQAM pull-request execution;
  native Actions check results and summaries remain available.
- Add twelve synthetic audit/workflow-policy regressions, security reporting
  guidance, third-party license pointers, review ownership and weekly Actions
  dependency-update proposals. No automatic dependency merges.

The 12 focused tests passed locally against the changed files. Consult the PR's
latest checks for the final remote result; the baseline scan numbers above are
intentionally tied to the stated earlier snapshot, not presented as timeless
counts. Added code is checked with the repository's Ruff rules in the audit job.

## Open items and limits

1. **Repository settings:** `main` was still unprotected when inspected at
   `086a84e3e755031a2dffa6ff2b28442549ac8894`. The connected tools do not expose an
   administration write action. Branch/ruleset protections, private vulnerability
   reporting, push protection and protected credential environments were not
   changed. CODEOWNERS and SECURITY.md do not enable these settings by themselves.
2. **Privacy coverage:** 111 binary blobs were not manually inspected. Older
   logs, artifact payloads, external links/forks, deleted refs, unreachable
   objects and PR review-submission bodies are outside the completed scan. A
   secret detector does not recognize all personal context or student records.
3. **Licensing:** Apache-2.0 is already present and is preserved. The MAT0339
   manual retains its noncommercial notice. Photo credits and institutional
   branding need purpose-specific rights/approval review. No broad asset-rights
   clearance was inferred.
4. **Onboarding and baseline CI:** the existing full-repository smoke lint is
   failing. The inspected packaging file specifies `setuptools.backends.legacy:build`;
   a supported-backend/clean-install check is still needed before advertising the
   editable install as verified. This hardening change does not refactor packaging,
   add an expanded end-to-end demonstration, or repair unrelated lesson lint.
5. **Remaining privileged workflows:** the historical audit-integration job and
   separately approved release publisher retain their trusted-branch write roles.
   The policy tests cover the five hardened workflows, not a universal proof that
   every possible future workflow is safe.

## Preservation

No lesson sources, curriculum, voice profiles, original books, photos, existing
rendered media or private personal archive were edited. No new narration was
requested and no publication or visibility change was made by this task. The
companion-preview release work on main belongs to the separately approved task.

## Interpretation

This is a successful bounded automated scan plus concrete hardening, not complete
privacy clearance, a legal opinion, institutional approval, or a production-ready
claim. Resolve the owner-controlled settings and remaining review items before a
broad invitation to reuse all repository content.
